Final Governance Corrections Before Authorizing Phase 8

The Phase 8A Readiness Audit has identified no scientific defects in the execution engine.

The statistical pipeline is frozen and approved.

No computation functions are to be modified.

No search-space logic, feature engineering, permutation testing, multiple-testing correction, or hypothesis evaluation may be changed.

The only remaining work is to complete the laboratory governance artifact.

Treat the downloadable JSON as the official immutable experiment record for the first scientific discovery campaign.

Its purpose is to allow an independent auditor, months or years later, to verify exactly what was executed without relying on browser IndexedDB or hidden application state.

Required Corrections

Modify only renderRecord() in msd-phase8-campaign.html.

Do not modify:

phase8-engine.js
server.js
index.html
any scientific function
any governance algorithm
1. Preserve the complete frozen search-space definition

The experiment record currently stores only:

searchSpaceId
searchSpaceHash
searchSpaceVersion

This is insufficient for independent verification.

Include the complete frozen search-space definition already available in the browser.

Use the object already stored in:

_seal.discoverySpec

Store it in the experiment record.

The search-space hash must therefore become independently re-computable from the downloaded artifact.

2. Preserve dataset fingerprint

The downloadable artifact must include the dataset fingerprint generated during the run.

Do not recompute it.

Read the value already returned by the server.

Use whichever field already exists, for example:

r.datasetFingerprint

or

r.frozenDatasetSnapshot.datasetFingerprint

Include the value unchanged in the JSON record.

This establishes permanent dataset identity.

3. Preserve hypothesis content hashes

Each hypothesis already contains:

contentHash

generated by:

msdFreezeHypothesisRecord()

Do not omit this field when building candidate_table.

Each candidate record should preserve its own scientific fingerprint.

This allows future auditors to verify that the stored p-values, effect sizes, adjusted p-values, and discovery decisions correspond exactly to the frozen hypothesis.